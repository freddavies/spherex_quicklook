# spherex_quicklook
Basic script for extracting spectrophotometry from SPHEREx cutouts (warning: science at your own risk)

Run `sh J0916-wget.sh` inside the J0916-2511 directory before running the `example.py` script.